# ⚠️ NEVER commit actual values in this file!
# Copy this file to .env.local and add your actual values there
# .env.local is gitignored and will never be committed

# Database Configuration (Neon PostgreSQL)
# Get your connection string from: https://neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Authentication Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here

# OAuth Providers (optional)
# Google OAuth - https://console.cloud.google.com/
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth - https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Authentication Mode
# Set to 'true' to bypass authentication (development only)
BYPASS_AUTH=false

# Environment
NODE_ENV=development

# Exchange Rate API (Free tier)
# No API key needed for free tier of exchangerate-api.com
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest/EUR
