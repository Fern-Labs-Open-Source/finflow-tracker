# Production Environment Variables Template
# These values should be set in your deployment platform (Netlify, Vercel, etc.)
# DO NOT commit actual production values to version control

# Database - Use connection pooling for serverless
DATABASE_URL=postgresql://[user]:[password]@[host]/[database]?sslmode=require&pgbouncer=true

# NextAuth - Generate unique secret for production
NEXTAUTH_SECRET=[generate-with-openssl-rand-base64-32]

# Must match your production domain
NEXTAUTH_URL=https://your-production-domain.com

# Production environment
NODE_ENV=production

# Authentication - Must be false in production
BYPASS_AUTH=false

# Optional Production Services
# MONITORING_ENABLED=true
# ERROR_TRACKING_ENABLED=true
# ANALYTICS_ID=
